# Lightweight runtime dependencies for constrained hosts (e.g. Render free
# tier 512 MB). Excludes the heavy ML runtimes (tensorflow, sentence-transformers,
# xgboost) which are only imported lazily and disabled via env flags
# (SEMANTIC_SEARCH_ENABLED=false, ENGAGEMENT_ML_ENABLED=false).
# For full local development, use requirements.txt instead.
fastapi
uvicorn
supabase
pydantic
pydantic-settings
python-dotenv
python-multipart
httpx
google-genai
pypdf
numpy
